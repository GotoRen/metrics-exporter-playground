```shell
### Build
$ docker build . -t ren1007/pushmetric-test:v2.1 --platform linux/x86_64

### Run
$ docker run --name pushmetric-test ren1007/pushmetric-test:v2.1

### Push
$ docker push ren1007/pushmetric-test:v2.1
```
